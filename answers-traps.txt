1.函数的参数保存在a0~a2寄存器中。在main对printf的调用中，寄存器a2保存13。
2.函数f的调用被编译器优化了，编译器直接计算出了f(8)+1的值；对g的调用在0x14到0x18。
3.0x64a。
4.返回地址0x38。
5.输出为He110 World。如果 RISC-V 是 big-endian，i需要设置为0x726c6400。不需要更改57616的值。
6.会打印出x=3,y=一个不确定的值。因为没有传递第二个参数，它的数值就取决于此前寄存器a2的值。
